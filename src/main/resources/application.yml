---
spring:
  application:
    name: "jemil-backend"
  datasource:
    url: "jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:jemil_db}"
    username: "${DB_USER:jemil}"
    password: "${DB_PASSWORD:jemil_secret}"
    driver-class-name: "org.postgresql.Driver"
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: "validate"
    show-sql: true
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.PostgreSQLDialect"
        format_sql: true
        default_schema: "public"
  liquibase:
    change-log: "classpath:db/changelog/db.changelog-master.xml"
    enabled: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: "${KEYCLOAK_ISSUER_URI:http://localhost:8081/realms/jemil}"
management:
  endpoints:
    web:
      exposure:
        include: "health, info, metrics"
  endpoint:
    health:
      show-details: "always"
logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  level:
    cm.jemil: "DEBUG"
    org.springframework: "INFO"
    org.hibernate.SQL: "DEBUG"
    org.springframework.security: "TRACE"
    net.ttddyy.dsproxy.listener: "DEBUG"
    org.hibernate.orm.jdbc.bind: "TRACE"
cors:
  allowed-origins: "${CORS_ALLOWED_ORIGINS:*}"
jwt:
  secret: "${JWT_SECRET:jemil-default-secret-key-change-in-production-32chars}"
  access-token-expiration: "${JWT_ACCESS_EXPIRATION:900000}"
  refresh-token-expiration: "${JWT_REFRESH_EXPIRATION:604800000}"
springdoc:
  api-docs:
    path: "/api-docs"
  swagger-ui:
    path: "/swagger-ui.html"
    operationsSorter: "method"
server:
  port: 8080
agency: null
