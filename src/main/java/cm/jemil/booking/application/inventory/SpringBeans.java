package cm.jemil.booking.application.inventory;

import org.springframework.context.annotation.Configuration;

/**
 * Spring configuration for booking inventory beans.
 * SeatHoldExecutor and SeatHoldService are auto-configured via @Service.
 */
@Configuration("bookingInventoryBeans")
public class SpringBeans {}
