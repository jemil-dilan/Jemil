---
openapi: "3.0.3"
info:
  title: "JEMIL Payment API"
  description: "Payment operations for the JEMIL platform.
    
    Placeholder health path retained until payment endpoints are defined.
    
    **Rule**: Any modification to this file requires review.
    Code is generated from this spec — not the other way around."
  version: "2026-W01"
  contact:
    name: "JEMIL Dev Team"
servers:
- url: "https"
- url: "http"
tags:
- name: "Health"
  description: "Service health"
paths:
  /payment/health:
    get:
      tags:
      - "Health"
      summary: "Payment module health placeholder"
      description: "Placeholder endpoint until payment APIs are defined."
      operationId: "paymentHealth"
      responses:
        "200":
          description: "Payment module is reachable"
components:
  schemas:
    CreationResponseDTO:
      type: "object"
      required:
      - "newId"
      properties:
        newId:
          type: "string"
          format: "uuid"
    ErrorDTO:
      type: "object"
      properties:
        timestamp:
          type: "string"
          format: "date-time"
        status:
          type: "integer"
        error:
          type: "string"
          description: "Code d'erreur technique"
          example: "DOMAIN_RULE_VIOLATION"
        message:
          type: "string"
          description: "Message lisible par un humain"
          example: "Le nom de l'agence est obligatoire."
